@inherits LayoutComponentBase

@Body

@code {
    // This layout is specifically for the chat interface
    // It provides a minimal layout that renders just the page content
    // The HTML document structure is handled by App.razor
}