{
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "npm run clean && npm run build:blazor && npm run docs:build-vitepress",
    "docs:build-vitepress": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "build:blazor": "dotnet publish ../LlmTornado.Docs.csproj -c Release -o ./blazor-dist /p:GHPages=true /p:WarningLevel=0 && cpr ./blazor-dist/wwwroot ./docs/public/playground -o",
    "clean": "rimraf ./docs/.vitepress/dist ./docs/public ./blazor-dist"
  },
  "keywords": [],
  "devDependencies": {
    "cpr": "^3.0.1",
    "rimraf": "^6.0.1"
  }
}
